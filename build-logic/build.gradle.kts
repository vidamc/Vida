plugins {
    `kotlin-dsl`
}

dependencies {
    // Доступ к плагинам из основного version catalog внутри конвенций не прямой;
    // здесь нужны артефакты-плагинов, чтобы применять их в precompiled-скриптах.
    // Пока конвенции опираются только на встроенные java-плагины — внешние
    // (spotless, errorprone) подключим, когда начнём их использовать.
}
