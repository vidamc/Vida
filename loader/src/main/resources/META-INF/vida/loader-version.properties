# Vida loader version.
# Filled by :loader:processResources via Gradle `expand` filter.
# Read at runtime by dev.vida.loader.internal.SyntheticProviders
# to publish the synthetic "vida" dependency provider.
version=${vidaVersion}
