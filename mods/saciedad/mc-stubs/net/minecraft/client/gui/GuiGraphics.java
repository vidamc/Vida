/*
 * Compilation stub only — NOT included in the saciedad JAR.
 * The real net.minecraft.client.gui.GuiGraphics is provided by Minecraft at runtime.
 *
 * API surface: only the methods used by InGameHudDispatchMorph.
 */
package net.minecraft.client.gui;

/**
 * Stub for {@code net.minecraft.client.gui.GuiGraphics} (Minecraft 1.20.5+).
 *
 * <p>Provides just enough API for {@code InGameHudDispatchMorph} to compile.
 * At runtime, the real class from the Minecraft JAR is used.
 */
public class GuiGraphics {

    /**
     * Draws a filled rectangle.
     *
     * @param minX   left edge (inclusive)
     * @param minY   top edge (inclusive)
     * @param maxX   right edge (exclusive)
     * @param maxY   bottom edge (exclusive)
     * @param color  color in ARGB format
     */
    public void fill(int minX, int minY, int maxX, int maxY, int color) {
        // stub — replaced by real implementation at runtime
    }
}
