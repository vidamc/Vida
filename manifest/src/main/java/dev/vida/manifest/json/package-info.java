/**
 * Vida-JSON: встроенный streaming JSON-парсер.
 *
 * <p>Используется для разбора {@code vida.mod.json} и других конфигурационных
 * файлов. Реализован вручную, без сторонних зависимостей, для предсказуемой
 * производительности и минимума аллокаций на hot path раннего бутстрапа.
 */
package dev.vida.manifest.json;
