# Profile legacy-121 / 1.21.7

Second baseline drop in the `1.21.x` semantic line. Same platform morph bundle and bridge class as `1.21.1`; differences at port time are Cartografía inputs (client mappings) and vanilla anchors, not the loader core.

See [platform-profiles README](../../README.md) for layout and validation.
