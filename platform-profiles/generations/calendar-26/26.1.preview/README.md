# Preview: calendar-26 / 26.1

Placeholder profile for the **calendar 26.x** platform generation (distinct SemVer rules from `1.21.*`). No stable client jar is assumed in-repo; this exists so tooling, CI, and documentation can reference `CALENDAR_26` before a real drop lands.

When the game ships, replace `gameVersion`, `mappingMode`, `morphBundle`, and bridge class as needed for that line.
